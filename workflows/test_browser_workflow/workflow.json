{
  "specification": "Test browser connectivity by navigating to a target URL defined by the 'url' parameter.",
  "states": [
    {
      "checks": [
        {
          "type": "always_true",
          "params": {}
        }
      ],
      "actions": [
        {
          "type": "go_to_url",
          "params": {
            "url": "{{url}}"
          }
        }
      ]
    }
  ],
  "parameters": ["url"]
}
