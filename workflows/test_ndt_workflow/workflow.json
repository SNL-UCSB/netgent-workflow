{
  "specification": "Perform a Network Diagnostic Test (NDT) to measure network performance, specifically testing 'download' and 'upload' speeds as configured.",
  "states": [
    {
      "checks": [
        {
          "type": "always_true",
          "params": {}
        }
      ],
      "actions": [
        {
          "type": "ndt",
          "params": {
            "download": "{{download}}",
            "upload": "{{upload}}"
          }
        }
      ]
    }
  ],
  "parameters": ["download", "upload"]
}
